# Base URL of the deployed backend API, including the /api prefix, e.g.:
#   VITE_API_BASE=https://support-agent-api.onrender.com/api
# Leave unset for local dev — Vite's dev proxy (vite.config.ts) forwards /api to localhost:8000.
VITE_API_BASE=
